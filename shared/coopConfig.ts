type coopConfigDef = {
    takeoverTimer: number,
    winTimer: number,

    coops: number,
}

// times in sec
export const coopConfig: coopConfigDef = {
    takeoverTimer: 2,
    winTimer: 20,

    coops: 1,
}